API_HOST=0.0.0.0
API_PORT=8080
DATABASE_MAX_CONNECTIONS=10
DATABASE_SCHEMA_VERSION=3
AUTH_SIGNING_KEY=replace-with-a-random-signing-key-at-least-32-bytes
AUTH_ISSUER=face-search-api
AUTH_AUDIENCE=face-search-web
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=720h
REFRESH_COOKIE_SECURE=false
WEB_ORIGIN=http://localhost:3000
QDRANT_COLLECTION=face-search-faces
FACE_AI_INTERNAL_TOKEN=
# NON-PRODUCTION test threshold only. Not a validated production value; the
# production face-search threshold is gated on the Checkpoint 1 benchmark.
SEARCH_NON_PRODUCTION_THRESHOLD=0.5
SEARCH_RESULT_LIMIT=100
# Controlled customer downloads. Signed GET links are short-lived and scoped to a
# single stored object; DOWNLOAD_URL_TTL bounds how long a link is valid.
DOWNLOAD_URL_TTL=2m
# Maximum photos per download request (bounds bulk downloads).
DOWNLOAD_MAX_BULK=50
# Per-window request budget for the public download endpoint, keyed by public
# token and client address.
DOWNLOAD_RATE_LIMIT=30
DOWNLOAD_RATE_WINDOW=1m
REDIS_STREAM=photo-jobs
OUTBOX_POLL_INTERVAL=2s
OUTBOX_BATCH_SIZE=50
OUTBOX_LEASE_TTL=30s
