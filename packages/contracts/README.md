# Contracts

`openapi.yaml` is the initial source of truth for the public Go API. It intentionally contains only the API-information and common error shapes. Event, Photo, Face, Auth and Search contracts will be added when their behavior is designed and implemented.
